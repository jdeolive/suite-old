analyticsCharts.performanceLine({
    container: "${container}",
    width : ${width},
    height : ${height},
    xlen : ${xlen},
    xdata : ${xdata},
    timeData : ${timeData},
    thruData : ${thruData},
    colors: [],
    xsteps : ${xsteps},
    breaks : ${breaks}
});
