analyticsCharts.line({
    container: "${container}",
    width : ${width},
    height : ${height},
    xlen : ${xlen},
    xdata : ${xdata},
    ydata : ${ydata},
    colors: ${colors},
    xsteps : ${xsteps},
    breaks : ${breaks}
});
